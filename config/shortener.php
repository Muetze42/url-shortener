<?php

return [

    /*
    |--------------------------------------------------------------------------
    | Visit Log Limit Hours
    |--------------------------------------------------------------------------
    |
    | This option controls how many hours must pass before a link that has
    | already been accessed by a device with the same browser data is evaluated
    | as a click again.
    |
    */

    'visit-log-limits-hour' => 12,

    /*
    |--------------------------------------------------------------------------
    | Referrer Log Limit Hours
    |--------------------------------------------------------------------------
    |
    | This option controls how many hours must pass before a referrer that has
    | already been accessed by a device with the same browser data is evaluated
    | as a click again.
    |
    */

    'visit-referrer-log-limits-hour' => 12,

    /*
    |--------------------------------------------------------------------------
    | Ref Parameter Log Limit Hours
    |--------------------------------------------------------------------------
    |
    | This option controls how many hours must pass before a ref parameter that
    | has already been accessed by a device with the same browser data is
    | evaluated as a click again.
    |
    */

    'visit-ref-log-limits-hour' => 12,
];
